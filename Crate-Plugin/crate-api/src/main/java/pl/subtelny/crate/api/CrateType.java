package pl.subtelny.crate.api;

import pl.subtelny.utilities.identity.BasicIdentity;

public class CrateType extends BasicIdentity<String> {

    public CrateType(String id) {
        super(id);
    }
}
